{
  "name": "mihomo-script",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@12.0.0",
  "scripts": {
    "build": "pnpm build:unmin && pnpm build:min",
    "build:unmin": "node scripts/build.mjs --minify=false",
    "build:min": "node scripts/build.mjs --minify=true",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2",
    "yaml": "^2.9.0"
  }
}
